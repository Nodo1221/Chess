spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/matchmaking_db
    username: admin
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
